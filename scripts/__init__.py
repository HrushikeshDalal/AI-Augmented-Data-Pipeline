"""
scripts/__init__.py

Makes the scripts directory a Python package so that
modules can be imported with:
    from scripts.utils import get_logger
    from scripts.extract import run_bronze_extraction
    etc.
"""
