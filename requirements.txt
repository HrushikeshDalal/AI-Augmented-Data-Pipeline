# ──────────────────────────────────────────────
# requirements.txt — Python Dependencies
# AI-Augmented Data Pipeline — Capstone Project
# ──────────────────────────────────────────────
# Install with: pip install -r requirements.txt
# Python 3.10+ required

# ── Core Data Engineering ──────────────────────
pandas>=2.0.0
numpy>=1.26.0
pyarrow>=14.0.0          # Parquet read/write
fastparquet>=2023.10.1   # Alternative Parquet engine

# ── Apache Airflow ─────────────────────────────
apache-airflow>=2.8.0
apache-airflow-providers-google>=10.0.0
apache-airflow-providers-slack>=8.0.0
apache-airflow-providers-http>=4.0.0

# ── dbt ────────────────────────────────────────
dbt-core>=1.7.0
dbt-bigquery>=1.7.0

# ── PySpark ────────────────────────────────────
pyspark>=3.5.0

# ── Google Cloud ───────────────────────────────
google-cloud-bigquery>=3.13.0
google-cloud-bigquery-storage>=2.24.0
google-cloud-storage>=2.13.0
google-auth>=2.25.0
db-dtypes>=1.2.0          # BigQuery ↔ pandas type bridge

# ── LLM / LangChain ───────────────────────────
langchain>=0.1.0
langchain-openai>=0.0.5
openai>=1.10.0

# ── HTTP / API ─────────────────────────────────
requests>=2.31.0
httpx>=0.26.0

# ── Visualisation ──────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

# ── Testing ────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
duckdb>=0.9.0             # In-memory SQL engine for dbt tests

# ── Code Quality ───────────────────────────────
ruff>=0.1.0               # Fast linter (replaces flake8 + isort)
black>=23.12.0            # Code formatter

# ── Utilities ─────────────────────────────────
python-dotenv>=1.0.0      # .env file loading
pyyaml>=6.0.1             # YAML parsing (dbt project files)
click>=8.1.7              # CLI helpers
tqdm>=4.66.0              # Progress bars

# ── Delta Lake (optional, for Spark+Delta) ────
# delta-spark>=3.0.0      # Uncomment if using Delta Lake storage format
